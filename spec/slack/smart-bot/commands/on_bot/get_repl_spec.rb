RSpec.describe SlackSmartBot, "get_repl" do
    describe "on channel bot" do
      channel = :cbot1cm
      user = :user1
  
      before(:all) do
        skip "Not added tests"
        #todo: add tests
      end

  end
end
  